# Trucking App Client

React + TypeScript client for the trucking app.

## Scripts

```bash
npm install
npm run dev
npm run lint
npm run build
```

The dev server proxies `/api` requests to the ASP.NET Core backend configured in `vite.config.ts`.
