# Frontend notes

- React forms use typed values and show inline API errors.
- Pickup dates are rendered as date-only values to avoid timezone shifts in the UI. Added validation to avoid selection of dates from the past.

