# Backend notes

- ASP.NET Core Identity endpoints are exposed under `/api`.
- Orders are visible only for the authenticated user who created them.

- Separated DTO logic from DB entity for Order entity.
- Extended server-side validation in addition to validation attributes

